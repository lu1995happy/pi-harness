# Host validation record

Host: Windows, Node 22.23.2, Pi 0.85.0, Herdr 0.8.2, WezTerm. Primary deployment target: macOS. These results do not establish macOS native-module, clipboard or mouse behavior.

## Completed checks

- Eighteen Node tests pass: map validation and dependencies, worktree isolation, independent-review enforcement, failed-launch recovery, no-holder cleanup and retry, reviewer replacement ordering, follow-up issues, crew todo transitions, fleet block detection, repository identity, durable acknowledgement and supervisor ownership, visual outbox replay, exact-HEAD gate evidence, shell quoting, annotation bindings, and installation settings/rollback preservation.
- Latest extension loaded and reloaded in Pi within the dedicated `pi-harness-review` Herdr session. `/harness` reports main role, global home and no active maps. Supervision status and the powerline footer render without startup errors.
- A separate temporary Herdr workspace confirmed directory switching with spaces/apostrophes using an output acknowledgement. Closing its final tab removed the workspace. The harness consequently waits for real successor tabs instead of creating holder tabs.

## Pi reviews

Two local Pi reviews reported concrete gaps, including PowerShell-only worker commands, repository matching from subdirectories, a two-attempt supervision cutoff, missing final-review skill, and workspace/worker startup lifetime problems. These findings informed the subsequent fixes and tests.

The third requested review read supervision, crew lifecycle, map gating and extension files and ran tests, then stopped with the provider error `The usage limit has been reached`. It did not produce the requested final report. No usage reset was redeemed. The newest code has not received a completed model-backed Pi review.

## Remaining evidence

- Run the full [macOS checklist](../macos-checklist.md), including real model-driven crew work and the no-mistakes gate.
- Check Apple Silicon/Intel native components on the actual target architecture.
- Complete the interrupted Pi code review when provider access is available.
- Validate visual fidelity and click/clipboard behavior in macOS WezTerm. Terminal text inspection on this host is not a screenshot-based visual approval.

The user chose a runnable checklist for their Mac instead of remote Mac access. No macOS installation is claimed in this record.
